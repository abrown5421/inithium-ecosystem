import { Button, Box, Text } from '@inithium/ui';

export function App() {
  return (
    
      <Box
        flex={{direction: 'row', justify: 'between', align: 'center', gap: 5}}
        bgColor={{color: 'surface', intensity: 100}}
        borderColor={{color: 'surface', intensity: 500}}
        padding={{base: 16}}
        margin={{top: 15}}
      >
        app
      </Box>
  );
}

export default App;
